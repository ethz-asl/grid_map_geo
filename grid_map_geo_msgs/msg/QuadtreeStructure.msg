# Snapshot of a grid_map_geo HashedWaveletQuadtree's actual multi-resolution
# leaf cells over some queried region, for visualization
# (grid_map_geo_rviz_plugin QuadtreeStructure display).
std_msgs/Header header

QuadtreeCell[] cells
